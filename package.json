{
  "name": "ogcoin-static-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/impact-checkout.js --bundle --format=iife --target=es2020 --outfile=js/impact-checkout.js",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@stellar/freighter-api": "6.0.1",
    "@stellar/stellar-sdk": "16.0.1",
    "lucide": "1.21.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
